{
  "name": "cross-chain-defi-aggregator-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for the Chainlink CCIP-powered cross-chain yield aggregator",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:mumbai": "hardhat run scripts/deploy.js --network polygonMumbai",
    "deploy:fuji": "hardhat run scripts/deploy.js --network avalancheFuji"
  },
  "keywords": [
    "chainlink",
    "ccip",
    "hardhat",
    "yield-aggregator"
  ],
  "author": "Andrew Walters",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^3.1.0"
  },
  "dependencies": {
    "@chainlink/contracts-ccip": "^1.6.4",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3"
  }
}
